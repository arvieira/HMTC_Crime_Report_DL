# Instructions

* Open the evaluation.py file and edit both the input dataset used for evaluation and the output dataset where the results will be stored.
* Run the evaluation script from the project root using `python Evaluation/evaluation.py`.
* Open the t_estimator.py file and edit both the input dataset used for evaluation and the output dataset where the results will be stored.
* Run the temperature scaling script from the project root using `python Evaluation/t_estimator.py`. 